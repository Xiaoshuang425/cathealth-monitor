{
  "name": "cathealth-app",
  "version": "1.0.0",
  "description": "CatHealth Monitor - Pet Health Tracking App",
  "main": "index.html",
  "scripts": {
    "build": "echo \"Building static files...\"",
    "sync": "npx cap sync",
    "android": "npx cap open android",
    "build:apk": "npm run sync && cd android && ./gradlew assembleDebug"
  },
  "keywords": ["cat", "health", "pet", "monitor"],
  "author": "CatHealth Team",
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0",
    "@capacitor/camera": "^5.0.0",
    "@capacitor/geolocation": "^5.0.0",
    "@capacitor/splash-screen": "^5.0.0",
    "@capacitor/status-bar": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
