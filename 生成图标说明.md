# 生成应用图标

## 方法一：使用在线工具（推荐）

1. 访问 https://romannurik.github.io/AndroidAssetStudio/icons-launcher.html
2. 选择 **Launcher Icon Generator**
3. 上传一张猫咪相关的图片（建议 512x512 PNG）
4. 调整样式（推荐选择 "Circle" 或 "Rounded Square"）
5. 点击下载 ZIP
6. 解压后将 `res` 文件夹内的内容复制到 `android/app/src/main/res/`

## 方法二：使用本地脚本

如果你有 1024x1024 的主图标，运行：

```bash
npm install -g @capacitor/assets
npx capacitor-assets generate --iconBackgroundColor '#D4A574' --splashBackgroundColor '#F8F4E9'
```

## 图标规格

需要生成以下尺寸的图标：

- `mipmap-mdpi` (48x48)
- `mipmap-hdpi` (72x72)
- `mipmap-xhdpi` (96x96)
- `mipmap-xxhdpi` (144x144)
- `mipmap-xxxhdpi` (192x192)

每个尺寸需要：
- `ic_launcher.png` - 主图标
- `ic_launcher_round.png` - 圆形图标
- `ic_launcher_foreground.png` - 前景（用于自适应图标）

## 临时方案

如果没有图标，可以使用默认的 Android 图标，或者创建一个简单的文字图标：

1. 创建一个 512x512 的 PNG 图片
2. 背景色: `#D4A574` (温暖的棕色)
3. 中间放置猫咪 Emoji: 🐱 或使用文字 "Cat"
4. 保存为 `icon.png`

然后使用在线工具生成所有尺寸。
