version: "1"
services:
  python-ai-service:
    type: web
    name: cathealth-monitor-1
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
